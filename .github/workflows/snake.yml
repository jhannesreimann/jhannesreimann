name: Generate contribution snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # real game of Snake: grows one segment per commit cell it eats,
      # hunts cells via BFS and avoids its own body.
      # https://github.com/dahan8473/snake-and-commits
      - name: Generate light variant
        uses: dahan8473/snake-and-commits@v1
        with:
          github_user: ${{ github.repository_owner }}
          output: dist/github-contribution-grid-snake.svg
          theme: green

      - name: Generate dark variant
        uses: dahan8473/snake-and-commits@v1
        with:
          github_user: ${{ github.repository_owner }}
          output: dist/github-contribution-grid-snake-dark.svg
          theme: matrix

      - name: Recolor to match profile card
        run: python3 scripts/recolor_snake.py dist/github-contribution-grid-snake.svg dist/github-contribution-grid-snake-dark.svg

      - name: Push to output branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: output
